# מסמך תכנון – סופר ומונה

> תכנון ארכיטקטורה וממשק. נכתב: 29.07.2026
>
> **הנחות יסוד:** רוב המשתמשים ב-**Windows** · Windows ומק זהים לחלוטין · אנדרואיד זהה בפונקציונליות ושונה בפריסה · תוכנה חינמית שמטרתה בסיס משתמשים נאמן.

---

## 1. שאלת הפתיחה: לכתוב מחדש או לשפר?

**ההמלצה: לשפר את הקיים, לא לכתוב מחדש.**

כתיבה מחדש נראית מפתה כשקובץ אחד מגיע ל-2,300 שורות, אבל היא כמעט תמיד המסלול היקר יותר. הסיבות כאן ספציפיות:

- **הלוגיקה העסקית היא הנכס.** חישובי ההספק והרווח לשלושה סוגי פרויקטים, הגימטריה, לוח השנה העברי, ימי העבודה – אלה נבנו מול מציאות אמיתית של סופרים ומכילים עשרות החלטות קטנות ונכונות. כתיבה מחדש תאבד חלק מהן בשקט.
- **הבעיה אינה הקוד אלא ארגונו.** אין כאן ארכיטקטורה שגויה – יש לוגיקה נכונה שיושבת בתוך `build()` במקום בשכבה משלה. זו בעיה שמתוקנת בהעברות, לא בכתיבה מחדש.
- **המשתמשים ממשיכים לעבוד.** רק סיימנו לתקן באגי אובדן נתונים. הקפאה של חודשים לכתיבה מחדש תעצור את הזרימה הזו.

**מה כן:** חילוץ הדרגתי בארבעה גלים, כשאחרי כל גל האפליקציה עובדת ונשלחת. הכיוון מפורט בפרק 3.

---

## 2. עקרונות מנחים

1. **הלוגיקה לא יודעת מה זה מסך.** כל חישוב הוא פונקציה טהורה שאפשר לבדוק בלי Flutter.
2. **פלטפורמה = יכולת, לא שם.** לשאול `PlatformSupport.isDesktop`, לא `Platform.isWindows`. זו הסיבה שמק נופל היום בין הכיסאות.
3. **מסך אחד, שתי פריסות.** אותו מסך ואותה לוגיקה; `LayoutBuilder` בוחר פריסה לפי רוחב. לא שני עצי מסכים נפרדים.
4. **דסקטופ קודם.** רוב המשתמשים בווינדוס. עיצוב שמתחיל בטלפון ונמתח למחשב נותן להם את החוויה הגרועה יותר.
5. **הצג מסקנה, לא רק מספר.** "42 שורות" הוא נתון. "42 שורות – 68% מהיעד, 61₪/שעה" הוא כלי עבודה.

---

## 3. ארכיטקטורת יעד

### מבנה תיקיות

```
lib/
├── main.dart
├── core/
│   ├── platform_support.dart      ✅ קיים
│   ├── theme.dart                 עיצוב מרוכז: צבעים, טיפוגרפיה, בהיר/כהה
│   └── app_icons.dart             מיפוי מושג → אייקון (פרק 5)
├── models/
│   ├── project.dart
│   ├── work_session.dart
│   └── expense.dart
├── logic/                         ← לב השינוי: פונקציות טהורות, נבדקות
│   ├── production_calculator.dart  הספק לפי סוג פרויקט
│   ├── profit_calculator.dart      רווח, ₪/שעה, נטו
│   ├── goal_calculator.dart        יעד יומי/חודשי, התקדמות
│   ├── date_logic.dart             שעת מעבר יום – מקור אמת יחיד
│   ├── hebrew_utils.dart          ✅ קיים
│   ├── work_days_calculator.dart  ✅ קיים
│   └── merge_service.dart          מיזוג נתונים (מחולץ מ-sync_service)
├── services/
│   ├── storage_service.dart       ✅ קיים
│   ├── backup_service.dart        ✅ קיים
│   └── notification_service.dart  ✅ קיים
├── state/
│   └── app_state.dart              מקור אמת יחיד לפרויקטים/היסטוריה/הוצאות
└── ui/
    ├── screens/
    ├── layouts/                    home_desktop.dart / home_mobile.dart
    └── widgets/                    רכיבים משותפים לשתי הפריסות
```

### ארבעת הגלים

| גל | תוכן | תוצאה |
|---|---|---|
| **1. חילוץ הלוגיקה** | להוציא את החישובים מ-`home_screen` / `summary_screen` / `project_summary_screen` ל-`logic/`. בלי שינוי התנהגות. | שכפול הלוגיקה בשלושה מסכים נעלם; אפשר לכתוב טסטים. פותר את א1, א3, א4 מ-[SUGGESTIONS.md](SUGGESTIONS.md) כתוצר לוואי, כי יהיה מקור אמת אחד. |
| **2. שכבת מצב** | `AppState` יחיד במקום העברת רשימות ב-constructor וקריאות חזרה. | מסכים מפסיקים להעביר נתונים זה לזה; שמירה וסנכרון במקום אחד. |
| **3. פריסות** | `LayoutBuilder` ברמת המסך, שתי פריסות לכל מסך ראשי. | חוויית דסקטופ אמיתית לרוב המשתמשים. |
| **4. עיצוב** | ערכת נושא מרוכזת, מצב כהה, אייקונים חדשים. | מראה אחיד ומודרני. |

**כלל:** בסוף כל גל האפליקציה נבנית, הטסטים עוברים, ואפשר לשחרר.

---

## 4. הממשק

### 4.1 מסך הבית – דסקטופ (Windows / מק)

הפעולה היומיומית צריכה לקרות במסך אחד בלי ניווט.

```
┌────────────────────────────────────────────────────────────────────┐
│  סופר ומונה          כ״ד בתמוז תשפ״ו    [🌙] [💾 נשמר] [☕] [⚙]   │
├──────────────────────────────┬─────────────────────────────────────┤
│                              │  היום                               │
│   ספר תורה – פרשת נח    ▾    │  ─────────────────────────────      │
│                              │  38 שורות     4ש 12ד               │
│        02:14:33              │  ████████████░░░░  68% מהיעד       │
│      ✒ כותב...               │  נשארו 18 שורות                    │
│                              │                                     │
│   עמוד יא · שורה 27          │  312₪  ·  61₪/שעה                 │
│                              │  ─────────────────────────────      │
│  ┌────────┐ ┌────────┐       │  הפרויקט                           │
│  │ עצור   │ │ הפסקה  │       │  ███████░░░░░░░░  47%              │
│  └────────┘ └────────┘       │  צפי סיום: י״ב באלול               │
│  ┌──────────────────────┐    │  ─────────────────────────────      │
│  │  ✓ סיימתי שורה       │    │  החודש                             │
│  └──────────────────────┘    │  64% מהיעד · 9 ימי עבודה נותרו     │
│  [ערוך מיקום] [הזנה ידנית]   │  צריך 3.2 עמודים ליום              │
├──────────────────────────────┴─────────────────────────────────────┤
│  [סיכומים]  [פרויקטים]  [הוצאות]  [הגדרות]                        │
└────────────────────────────────────────────────────────────────────┘
```

**מה השתנה מהותית:** העמודה הימנית עונה בלי לחיצה אחת על שלוש השאלות – כמה כתבתי, כמה הרווחתי, ומה נשאר. `₪/שעה` מופיע במקום שבו הוא רלוונטי.

**נקודת החלטה – שורת ניווט תחתונה בדסקטופ:** זה דפוס מובייל. בדסקטופ מקובל סרגל צד קבוע או תפריט עליון. הצעתי: **סרגל צד צר משמאל** (אייקון + תווית), שגם משאיר את הניווט גלוי תמיד וגם מפנה גובה.

### 4.2 מסך הבית – אנדרואיד

אותן פונקציות, פריסה אנכית, אצבע אחת:

```
┌──────────────────────┐
│ ☰  סופר ומונה    ☕ │
│  כ״ד בתמוז תשפ״ו     │
├──────────────────────┤
│  ספר תורה – נח   ▾   │
│                      │
│      02:14:33        │
│      ✒ כותב...       │
│  עמוד יא · שורה 27   │
│                      │
│ ┌──────────────────┐ │
│ │ ✓ סיימתי שורה    │ │  ← יעד גדול, אצבע
│ └──────────────────┘ │
│ [ עצור ] [ הפסקה ]   │
├──────────────────────┤
│ היום  38 שורות 4:12  │
│ ███████████░░░ 68%   │
│ 312₪ · 61₪/שעה       │  ← אותו מידע, מקוצר
├──────────────────────┤
│ 🏠   📊   📁   ⚙     │
└──────────────────────┘
```

**עקרון:** הטלפון משמש בעיקר תוך כדי כתיבה – הכפתור הגדול והמצב הנוכחי למעלה. הניתוח המלא נשאר במסך הסיכומים.

### 4.3 נקודת השבירה

רוחב אחד: **900px**. מעליו – שתי עמודות; מתחתיו – עמודה אחת. חלון מוקטן בווינדוס מקבל את פריסת המובייל, וזה נכון: הפריסה עוקבת אחרי המקום, לא אחרי מערכת ההפעלה. כך גם המק מקבל את הפריסה הנכונה ביום שייבנה, בלי שורת קוד ייעודית.

---

## 5. אייקונים

### הבעיה הנוכחית

האפליקציה משתמשת ב-Material Icons הקלאסיים (`Icons.play_arrow`, `Icons.speed`, `Icons.folder_special`) בקווי עובי מעורבים ובלי מיפוי עקבי – מושגים דומים מקבלים אייקונים שונים במסכים שונים.

### ההמלצה: Material Symbols

**חבילה:** `material_symbols_icons` – הדור הנוכחי של מערכת האייקונים של Google. יתרונות:

- **משתנה** – ניתן לכוון עובי, מילוי ודרגת אופטיות. אפשר להשתמש במילוי כדי לסמן מצב פעיל (טאב נבחר מלא, שאר הטאבים קו) – רמז ויזואלי שהאפליקציה לא מנצלת היום.
- **3 סגנונות** – Outlined / Rounded / Sharp. **המלצה: Rounded** – ידידותי, מתאים לכלי עבודה יומיומי, משתלב עם `Colors.deepPurple` הקיים.
- **כיסוי רחב** – יש אייקון לכל מושג במפה למטה בלי לחפש חלופות.
- **מותאם RTL** – חשוב לממשק עברי; אייקונים כיווניים מתהפכים נכון.

חלופה לשקילה: **Lucide** (`lucide_icons`) – קו נקי ואחיד, מראה מודרני יותר ופחות "גוגלי". פחות כיסוי, ופחות מותאם ל-RTL. אם המטרה היא זהות ויזואלית מובחנת – זו הבחירה.

### מפת מושג → אייקון

טבלה אחת שמרכזת את כל האייקונים, כדי שאותו מושג ייראה זהה בכל מסך. תיושם ב-`core/app_icons.dart`.

| מושג | Material Symbol | הערה |
|---|---|---|
| ספר תורה | `menu_book` | לא `folder_special` |
| תפילין | `inventory_2` | סט/קופסה |
| מזוזה | `door_front` | ייחודי ומזוהה |
| התחל טיימר | `play_arrow` (filled) | |
| עצור | `stop_circle` | |
| הפסקה | `coffee` | ✅ כבר בשימוש |
| סיימתי שורה | `check_circle` | הפעולה החוזרת ביותר |
| כתיבה פעילה | `edit_note` | מחליף את אייקון הנוצה |
| הזנה ידנית | `keyboard` | |
| ערוך מיקום | `my_location` | |
| סיכום יומי | `today` | |
| סיכום חודשי | `calendar_month` | |
| סיכום פרויקט | `analytics` | |
| רווח / כסף | `payments` | |
| ₪ לשעה | `speed` | משחרר את `speed` מ"מצב חכם" |
| הוצאות | `receipt_long` | ✅ כבר בשימוש |
| יעד | `flag` | |
| הושג | `verified` | |
| מצב חכם | `auto_awesome` | |
| גיבוי | `backup` | ✅ כבר בשימוש |
| שחזור / ייבוא | `restore_page` | |
| שיתוף | `share` | |
| סל מחזור | `delete_sweep` | |
| הגדרות | `settings` | |
| מצב כהה | `dark_mode` | |
| תרומה | `volunteer_activism` | חם יותר מ-`coffee` |

### אייקון האפליקציה

`assets/icon/ICON.png` – שווה רענון בהתאם לכיווני העיצוב של שתי המערכות:
- **Windows 11** – פינות מעוגלות, עומק עדין
- **macOS** – מסגרת squircle, נדרשות כל מידות ה-`.icns`
- **Android** – **Adaptive Icon** (שכבת רקע + חזית נפרדות). היום יש PNG יחיד, ולכן האייקון עלול להיחתך במשגרים שונים. זהו פער אמיתי שכדאי לסגור.

---

## 6. ערכת נושא

```dart
// core/theme.dart – מקור אמת אחד לצבע ולטיפוגרפיה
seed:        deepPurple            // שמירה על הזהות הקיימת
surface:     לפי בהיר/כהה          // במקום 18 שימושי Colors.white
parchment:   0xFFFFF5E6            // רקע הקלף – קבוע בשם, לא בערך גולמי
success:     ירוק   – יעד הושג
warning:     ענבר  – פיגור מהיעד
danger:      אדום  – הפסד / שגיאה
```

**טיפוגרפיה:** גופן עברי מוטמע כדי שהמראה יהיה זהה ב-Windows, במק ובאנדרואיד. היום כל פלטפורמה מציגה גופן אחר (Segoe UI / SF Hebrew / Roboto). מומלצים: **Assistant** או **Heebo** – שניהם ברישיון OFL וחופשיים לשימוש מסחרי.

היררכיה: תצוגת הטיימר גדולה ומונו-מרווח (ספרות לא "קופצות"), כותרות מודגשות, גוף רגיל, הערות מוקטנות ומעומעמות.

---

## 7. מפת דרכים משולבת

| שלב | מ-DESIGN_PLAN | מ-SUGGESTIONS | תוצר למשתמש |
|---|---|---|---|
| **א** | גל 1 – חילוץ לוגיקה | א1, א3, א4, א12 | תאריכים וחישובים נכונים; בסיס לייבוא |
| **ב** | – | ייבוא + א2, א7 | מעגל גיבוי סגור |
| **ג** | גל 2 – שכבת מצב | ב1, ב2, ג1 | ביטול פעולות, חיווי שמירה, ₪/שעה |
| **ד** | גל 3 – פריסות | ב3, ב4 | חוויית דסקטופ לרוב המשתמשים |
| **ה** | גל 4 – עיצוב | ב5, ב8 | אייקונים, מצב כהה, גופן אחיד |
| **ו** | – | ג2, ג3, ג5 | מ"מונה" ל"יועץ" |

---

## 8. תלויות שיידרשו

| חבילה | לצורך | שלב |
|---|---|---|
| `material_symbols_icons` | מערכת אייקונים | ה |
| גופן Assistant/Heebo | טיפוגרפיה אחידה | ה |
| `provider` *או* `flutter_riverpod` | שכבת מצב | ג |
| `fl_chart` | גרפים (מחליף ציור ידני) | ו |

**הערה על ניהול מצב:** גם `AppState` פשוט עם `ChangeNotifier` + `provider` יספיק כאן. אפליקציה חד-משתמשית עם מסך ראשי דומיננטי לא מצדיקה תשתית כבדה יותר – זו הייתה החלטה נכונה מלכתחילה, והיא נשארת נכונה.

---

## מסמכים קשורים
- [SUGGESTIONS.md](SUGGESTIONS.md) – התנגשויות והצעות, ממוינות לפי עדיפות
- [DOCUMENTATION.md](DOCUMENTATION.md) – תיעוד טכני של המצב הקיים
- [CHANGELOG.md](CHANGELOG.md) – יומן שינויים

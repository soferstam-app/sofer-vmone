# סופר ומונה (Sofer v'Mone) 📜

אפליקציה מתקדמת לניהול, מעקב ותיעוד עבודת סופר סת"ם.
האפליקציה מאפשרת לסופרים לנהל פרויקטים של כתיבת ספרי תורה, תפילין ומזוזות, לעקוב אחר זמני עבודה, לחשב הספקים ורווחים, ולצפות בסטטיסטיקות מפורטות.

![App Icon](assets/icon/icon.png)

## ✨ תכונות עיקריות

### ⏱️ ניהול זמן וכתיבה
*   **טיימר חכם:** מדידת זמן כתיבה מדויקת עם אפשרות לעצירה והמשך.
*   **הזנה ידנית:** אפשרות להוספת רשומות כתיבה בדיעבד (כולל תאריך ושעות).
*   **תאריך עברי:** תצוגה מלאה של התאריך העברי בכל מסכי האפליקציה.

### 📚 ניהול פרויקטים
תמיכה מלאה בשלושה סוגי פרויקטים עם לוגיקה ייחודית לכל אחד:
1.  **ספר תורה:**
    *   מעקב לפי עמודים ושורות.
    *   הגדרת מספר עמודים ושורות לעמוד.
    *   תצוגה ויזואלית (גריד) של התקדמות הכתיבה בכל עמוד (מילוי לפי אחוזים).
2.  **תפילין:**
    *   ניהול סטים (ראש + יד), או פרשיות בודדות.
    *   מעקב אחר 4 הפרשיות (קדש, והיה כי יביאך, שמע, והיה אם שמוע).
    *   חישוב אוטומטי של "זוגות" וסטים שנשלמו.
3.  **מזוזות:**
    *   מעקב לפי יחידות ושורות (לפי תקן של 22 שורות למזוזה).

### 📊 סיכומים וסטטיסטיקה
*   **סיכום יומי:** הצגת הספק יומי, זמן עבודה, רווח נקי ועמידה ביעדים.
*   **סיכום חודשי:** טבלה מרכזת של כל העבודה בחודש העברי, כולל חישוב ימי עבודה בפועל.
*   **סיכום פרויקט:** מבט על התקדמות הפרויקט כולו, כולל ממוצע דקות לשורה/פרשייה וצפי סיום.
*   **חישוב רווחים:** הזנת מחיר והוצאות לכל פרויקט וקבלת דוח רווח נקי בזמן אמת.

### ⚙️ הגדרות וגיבוי
*   שמירת נתונים מקומית ומאובטחת.
*   **גיבוי ל-Google Drive** – סנכרון פרויקטים, היסטוריה והוצאות (בהתחברות Google).
*   אפשרות לאיפוס נתונים מלא.
*   ממשק נקי ונוח לשימוש בעברית.

## 🛠️ טכנולוגיות

האפליקציה פותחה באמצעות **Flutter** ומשתמשת בספריות הבאות:
*   `kosher_dart`: לחישובי לוח שנה עברי ותאריכים.
*   `shared_preferences`: לשמירת נתונים מקומית במכשיר.
*   `url_launcher`: לפתיחת קישורים חיצוניים.
*   `flutter_launcher_icons`: ליצירת אייקונים מותאמים.

## 📥 הורדה והתקנה

ניתן להוריד את הגרסה האחרונה מדף ה-Releases בגיטהאב:
**[קישור להורדות]**
> הערה: עליך לעדכן את הקישור לאחר שינוי שם המאגר (repository) בגיטהאב.
### אנדרואיד (Android)
1.  הורד את קובץ ה-`apk`.
2.  התקן אותו על המכשיר שלך (ייתכן שתצטרך לאשר התקנה ממקורות לא ידועים).

### ווינדוס (Windows)
1.  הורד את קובץ ה-`zip`.
2.  חלץ את התיקייה למחשב.
3.  הפעל את הקובץ `sofer_vmone.exe` מתוך התיקייה.  
    **החבילה כוללת את ספריות ה-VC++ Runtime** – אין צורך בהתקנת "Microsoft Visual C++ 2015–2022 Redistributable" בנפרד.

## 🔑 גיבוי ל-Google Drive (סנכרון)

*   **אנדרואיד:** הגיבוי עובד "מהקופסה". האימות מתבצע מול Google (חתימת האפליקציה מוגדרת ב-Google Cloud Console) – אין צורך להכניס מפתחות לקוד. קמפול רגיל (`flutter build apk`) כולל גיבוי.
*   **ווינדוס:** הגיבוי קורא מפתחות OAuth מאחד מהמקורות הבאים (בסדר עדיפות):
    1. **קובץ מקומי (מומלץ):** שים קובץ `oauth_credentials.json` בתיקיית הפרויקט (שורש המאגר) או **ליד ה-exe** (בתיקיית `build\windows\x64\runner\Release\` אחרי בנייה). הקובץ לא עולה לגיטהאב (מופיע ב-.gitignore). יש קובץ דוגמה `oauth_credentials.json.example` – העתק ל-`oauth_credentials.json` ומלא את המפתחות.
    2. **בעת הקמפול:** `flutter build windows --dart-define=GOOGLE_OAUTH_CLIENT_ID=... --dart-define=GOOGLE_OAUTH_CLIENT_SECRET=...`  
    אם אין קובץ ואין dart-define, כפתור "Sign In Google" לא יבצע התחברות. מפתחות יוצרים ב-[Google Cloud Console](https://console.cloud.google.com/) – סוג לקוח: "Desktop app".

## 🤝 תרומה ופיתוח

פרויקט זה הוא קוד פתוח. נשמח להצעות ייעול, דיווחי באגים ובקשות למשיכה (Pull Requests).

כדי להריץ את הפרויקט אצלך:
```bash # TODO: עדכן את הקישור לאחר שינוי שם המאגר
git clone https://github.com/YOUR_USERNAME/sofer-vmone.git
cd sofer-vmone
flutter pub get
flutter run
```

---
נבנה באהבה עבור סופרי הסת"ם. ✍️

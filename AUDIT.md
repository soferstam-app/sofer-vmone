# סקירה — שגיאות והמלצות לשדרוג

> **מקרא:** ✅ בוצע · ⬜ פתוח · 👤 עליך

**עודכן 4.8.2026, גרסה 0.4.0+3.**
‏658 בדיקות עוברות · `flutter analyze` נקי על `lib` ו-`test`.

מסמך החלטה. הפריטים המסומנים ✅ **בוצעו, נבדקו ונדחפו**; השאר ממתינים להחלטתך.

איך לקרוא את עמודת **הביסוס**:
- **הורץ** — הרצתי את הקוד וראיתי את התוצאה.
- **נקרא** — נובע מקריאת הקוד. בטוח, אך לא הודגם בהרצה.
- **הערכה** — שיפוט מקצועי, לא ממצא.

---

## תקציר — מה הייתי עושה, לפי הסדר

| מצב | פריט | הערה |
|---|---|---|
| ✅ | **ח1** מחיר עם פסיק נשמר כאפס | תוקן ב-`ea649df`, כולל הוצאות והצעת מחיר |
| ✅ | **ח3** "המשך" אחרי הפסקה | תוקן ב-`ea649df`, עם בדיקה |
| ✅ | **ח2** חלון ווינדוס | תוקן ב-`ea649df` + `9f207e8` |
| ✅ | **ט1** רק זמן שנמדד נכנס ליחסים | תוקן — ראו הכלל למטה |
| ✅ | **ט2 ט3 ט4** | תוקנו |
| ⬜ | **א1** רשת ב-macOS | דקות |
| ✅ | **ה1** CHANGELOG | נכתב מחדש ל-0.4.0; הסעיפים המיושנים נוקו |
| ⬜ | **ג1** המרות לוח עברי | 20 דק׳ |
| ⬜ | **ב1** "סיימתי שורה" בקלף/לילה | 20 דק׳ |
| ⬜ | **ד1** נגישות | 2–3 שעות |
| ⬜ | **ה2 ה3 ה4 ד2 ד3** | ניקיון ושיפורים |
| 👤 | **רוטציית המפתח** | `docs/SIGNING.md` · רק אתה |

## הכלל שנקבע לזמן

**כל יחס שיש בו זמן** — דקות לשורה, ₪ לשעה — מחושב **רק מרשומות שנמדד בהן זמן**, משני צדי היחס.
**כל מספר כללי** — סך שורות, רווח, התקדמות, צפי סיום — מחושב **מהכול**.

היכן שחלק מהעבודה בלי זמן, המסך אומר זאת במקום להציג מספר שנראה מלא.

---

# א. שגיאות

## א1 · בדיקת העדכונים לא יכולה לעבוד ב-macOS

**ביסוס: נקרא** · `macos/Runner/Release.entitlements`

**זהו בדיוק אותו באג שתוקן באנדרואיד**, בפלטפורמה אחרת. הקובץ מכיל הרשאה אחת בלבד:

```xml
<key>com.apple.security.app-sandbox</key>
```

אין `com.apple.security.network.client`, ובלעדיה אפליקציה בארגז חול לא יכולה לפתוח חיבור יוצא. `UpdateService` ייכשל — ויתפס ב-`catch` הגורף ויציג "לא הצלחתי לבדוק עדכונים כרגע", בדיוק כמו באנדרואיד.

חמור מזה: **גם `DebugProfile.entitlements` חסרה אותה** (יש בה `network.server` בלבד, שהיא של כלי Flutter). כלומר הבדיקה תיכשל גם בפיתוח — כך שגם מי שיבנה למק לא ייתקל בסימן שמשהו לא בסדר, אלא באותה הודעה מנומסת.

**מצב:** לא נבנה למק כרגע, ולכן זה תיאורטי — עד לבנייה הראשונה.
**מאמץ:** דקות. שתי שורות בשני קבצים.

## א2 · `otherCurrencies` אינו מה שהשם ומהתיעוד אומרים

**ביסוס: הורץ** · `lib/logic/annual_report.dart:23`

התיעוד: *"מה שהאפליקציה לא יכלה לקפל לתוך המספרים, מפני שהוא במטבע אחר"*. בפועל השדה אוסף **כל** מטבע שנמצא, כולל הראשי. בשנה רגילה בשקלים הוא מכיל `{ILS}`.

ההתנהגות מכוונת ובדוקה (`annual_report_test.dart:182`), **והשם והתיעוד הם השגויים**. השדה גם אינו בשימוש בשום מסך, ולכן זה לא פוגע במשתמש היום — אבל מישהו יקרא את השם, יאמין לו, ויציג "יש סכומים שלא נכללו" למי שהכול אצלו נכלל.

**המלצה:** לשנות שם ל-`currenciesPresent` ולתקן את התיעוד. **מאמץ: 10 דקות.**

## א3 · `MonthlyReport.earned` — תיעוד שמתאר טיפוס אחר

**ביסוס: נקרא** · `lib/logic/monthly_report.dart:28`

*"Null כשהחודש פורש על פרויקטים בכמה מטבעות"* — אבל השדה הוא `MoneyTotal` ואינו nullable לעולם. שאריות מגלגול קודם.

**מאמץ: דקה.**

---

# ב. פערים ואי־עקביות

## ב1 · "סיימתי שורה" קיים במודרני, חסר בקלף ובלילה

**ביסוס: הורץ** · `HomeActions` ב-`lib/home/ruled_home_body.dart:77`

`HomeActions` מחזיק `onNextLine` (מצב חכם) אבל **אין בו `onLap` כלל**. לכן:

| | מצב רגיל | מצב חכם |
|---|---|---|
| מודרני | ✅ יש | ✅ |
| קלף / לילה | ❌ **אין** | ✅ |
| חלון צף (ווינדוס) | ✅ יש | — |

**זה הפרש ביכולת ולא בעיצוב** — ובדיוק מה ש-`theme_parity_test.dart` נכתב כדי למנוע, אלא שהוא בודק נתונים ולא פעולות. סופר שיעבור לקלף יגלה שפעולה שהייתה לו נעלמה, ולא יבין למה.

**המלצה:** להוסיף `onLap` ל-`HomeActions` ולהציג בשתי הפריסות. **מאמץ: 20 דקות.**

## ב2 · צבעים קשיחים במסך הבית המודרני

**ביסוס: נקרא** · 36 מופעים ב-`lib`, מהם 14 ב-`home_screen.dart` ו-7 ב-`floating_window.dart`

`Colors.deepPurple`, `Colors.green[400]`, `Colors.brown.shade800`, `Color(0xFFFFF5E6)` ועוד. הערכה המודרנית **אמורה** לתמוך במוד כהה לפי המערכת, וטקסט סגול על רקע כהה הוא בעיית ניגודיות.

כבר תוקן בסבב קודם מקרה אחד מהמשפחה הזאת — רקע `0xFFFDF7FF` שהפך את המסך ללבן במוד כהה. **אלה השאריות.**

**המלצה:** להעביר ל-`ColorScheme` של הערכה. **מאמץ: שעה.** כדאי לבדוק ידנית במוד כהה לפני.

---

# ג. ביצועים

אף אחד מאלה אינו מורגש היום. **כולם יורגשו** כשלמשתמש יהיו שנתיים־שלוש היסטוריה — כלומר בדיוק אצל המשתמשים הוותיקים ביותר.

## ג1 · המרת לוח עברי לכל רשומה, פעמיים

**ביסוס: נקרא** · `lib/logic/date_logic.dart:132`

```dart
static bool sessionIsInMonth(WorkSession session, DateTime month, DayStart dayStart) {
  final a = JewishDate.fromDateTime(workingDateOf(session, dayStart));
  final b = JewishDate.fromDateTime(month);   // ← מחושב מחדש בכל קריאה
  ...
```

הפונקציה נקראת בלולאה על כל ההיסטוריה (`monthly_report.dart:79`). ל-2,000 רשומות זה **4,000 המרות לוח מלאות** לכל בניית דוח — ומחציתן מחשבות שוב ושוב את אותו חודש בדיוק.

זה גם סותר עיקרון שהפרויקט כתב לעצמו במפורש ב-`hebrew_work_calendar.dart`: *המרה אחת פנימה, קידומים זולים, המרה אחת החוצה.*

**המלצה:** להוציא את `b` החוצה, או להוסיף גרסה שמקבלת `JewishDate` מוכן. **מאמץ: 20 דקות. הזול והמשתלם ביותר ברשימה.**

## ג2 · `ProductionPlan` סורק את כל ההיסטוריה פעמיים ליום

**ביסוס: נקרא** · `lib/logic/production_plan.dart:207` ו-`:243`

`_unitsUpTo` עוברת על כל הרשומות, והיא נקראת **פעם בכל יום בטווח, בשתי לולאות נפרדות**. לחודש עם 2,000 רשומות: ‏~120,000 פעולות בכל בנייה — ובנייה קורית בכל `setState`.

**המלצה:** לחשב סכום מצטבר פעם אחת. **מאמץ: שעה.**

---

# ד. נגישות ושימושיות

## ד1 · אין ולו `Semantics` אחד בכל האפליקציה

**ביסוס: הורץ** · 0 מופעים של `Semantics(`, ‏`semanticLabel`, ‏`excludeSemantics`

כל כפתור־אייקון ללא תווית טקסט הוא, לקורא מסך, **"כפתור" ותו לא**. יש כאלה: הכלים ב-AppBar, עריכת המיקום, פעולות ברשימות.

**קהל היעד של האפליקציה מבוגר ברובו.** זו אינה תוספת אלא תנאי שימוש עבור חלק מהמשתמשים.

**מאמץ: 2–3 שעות** לכיסוי הגון של המסכים המרכזיים.

## ד2 · אין התייחסות לגודל טקסט מערכתי

**ביסוס: הורץ** · 0 מופעים של `textScaler` / `textScaleFactor`

משתמש שהגדיל את גודל הגופן במערכת — נפוץ מאוד בגיל מבוגר — יקבל פריסות שנשברות. יש בקוד `fontSize` קשיחים רבים (80 לשעון, 24, 21, 18), חלקם בתוך `Row` צר. `FittedBox` מגן על השעון בלבד.

**המלצה:** לבדוק ידנית ב-200% ולתקן את מה שנשבר. **מאמץ: חצי יום.** אני יכול לרנדר את המסכים בכמה גדלים ולהראות מה נשבר, לפני שמחליטים.

## ד3 · מסך ראשון — אין הכוונה

**ביסוס: הערכה**

תוקן החלק הגרוע (מסך ריק + בורר פרויקט), אבל משתמש חדש עדיין נוחת על שעון ומגלה את שאר האפליקציה לבד. **אין הסבר מה זה "מצב חכם", מה זה "הזנה ידנית", ולמה כדאי להגדיר ימי עבודה.**

**המלצה:** שלושה־ארבעה מסכי פתיחה חד־פעמיים, או "מה חדש" בעדכון. **מאמץ: יום.**

---

# ה. ניקיון לפני שחרור

| # | פריט | מצב | מאמץ |
|---|---|---|---|
| ✅ **ה1** | `CHANGELOG.md` — **בוצע.** הוסר "(בפיתוח)", נוספו הלוח החודשי, הדוחות, ההדפסה, טופס ההזנה, התזכורת, הגיבוי, המטבע, חלון ווינדוס וכל התיקונים; והוסר סעיף "גרסה נוכחית (פיתוח)" שישב מתחת ל-0.3.0 | — | — |
| **ה2** | `README.md` — `YOUR_USERNAME` בהוראות ה-clone | מופע אחד | דקה |
| **ה3** | `SUGGESTIONS.md` — ג1–ג5, ג8–ג10 מסומנים פתוחים ובוצעו | מטעה אותך, לא את המשתמשים | 20 דקות |
| **ה4** | `DOCUMENTATION.md` פרק 12 — "שינויים שטרם נכנסו לגיט", מתייחס ל-`b5270e4` (v0.3.0) | פרק שלם שאינו נכון | חצי שעה |
| **ה5** | 54 חבילות עם גרסאות חדשות יותר | לא דחוף. **לא לפני שחרור** | — |

---

# ו. המלצות לשדרוג

## ו1 · מעקב הגהה *(ג7 בקובץ ההצעות)*

**השלב היחיד בעבודה שהתוכנה מתעלמת ממנו לגמרי** — ויש לו עלות שאתה כבר רושם: "הגהות מזוזות" ו"הגהות תפילין" קיימות כקטגוריות הוצאה.

נכתב ← נשלח למגיה ← חזר ← תוקן. שדות: תאריך שליחה, שם המגיה, עלות, מה חזר.

**כאן כל עבודת פורמט השמירה משתלמת** — הסכימה החדשה תיכנס בלי לשבור אף גיבוי קיים. **מאמץ: 2–3 ימים.** ההמלצה החזקה שלי מבין התוספות.

## ו2 · לקוחות והזמנות *(ג6)*

היום יש `clientEmail` בודד. מי הלקוח, מה סוכם, מה שולם, מה נותר. **המטבע לרשומה שנוסף בגרסה הזו הוא חצי מהתשתית לזה.** **מאמץ: 3–4 ימים.**

## ו3 · חיפוש בהיסטוריה

**ביסוס: נקרא** — אין חיפוש בשום מסך.

סופר עם שנתיים עבודה שרוצה למצוא "מתי כתבתי את עמוד קמ״ה" גולל. **מאמץ: יום.**

## ו4 · ביטול פעולה אחרונה

מחיקת רשומה עוברת לסל מיחזור, אבל **הזנה שגויה** דורשת מציאה ועריכה. "בטל" לכמה שניות אחרי שמירה. **מאמץ: חצי יום.**

## ו5 · ייצוא CSV גולמי לרואה חשבון

יש PDF ואקסל מעוצבים. **רואה חשבון רוצה עמודות גולמיות** שאפשר להזין לתוכנה שלו. התשתית (`ExportTable.toCsv`) כבר קיימת. **מאמץ: שעתיים.**

## ו6 · מסלול לגוגל פליי

היום ההפצה בגיטהאב, וכל מתקין פוגש אזהרת Play Protect. **המסלול היחיד שמסיר אותה** הוא חנות — ולשם צריך `applicationId` אמיתי ומפתח אמיתי.

התוכנית הדו־שלבית: **0.4.0 נשארת `com.example`** (משתמשי 0.3.x מתעדכנים חלק ומקבלים ייצוא), **0.5.0 מחליפה `applicationId`** כשלכולם כבר יש ייצוא. **המסלול הזה נוצר בזכות הגיבוי שנבנה בגרסה הזו.**

## ו7 · העברה ברשת מקומית *(ג11)*

טלפון ומחשב באותה רשת, בלי קובץ ובלי ענן. **מאמץ: 3–4 ימים.** נחמד, לא דחוף — הגיבוי לקובץ כבר פותר את הצורך.

---

# ז. מה נבדק ונמצא תקין

לא הכול צריך תיקון, וכדאי לדעת מה כבר מוצק:

- **`MergeService`** — שני רגיסטרי מצבה, מיזוג לפי id, שום רשומה לא נופלת. `loadProjects()` **אינו** מסנן מחוקים, ולכן תרחיש התחייה שחיפשתי אינו קיים.
- **`_saveList`** — שמירה לעולם אינה מוחקת בהשמטה.
- **`IdGenerator`** — חותמת + 32 ביט אקראיות, עמיד להתנגשות בין מכשירים.
- **`SmartPosition.after`** — הגלגול לעמוד הבא נכון, כולל אחרי השורה האחרונה.
- **`TimerController`** — שעון מונוטוני למדידה, שעון קיר רק לפער שהתהליך לא שרד. תכנון נכון.
- **בורר החודש** מטפל באדר א׳/ב׳, כולל מעבר משנה מעוברת לפשוטה.
- **`MoneyTotal`** — לא מחבר מטבעות שונים, ומחזיר `null` במקום לשקר.
- **הרשאות אנדרואיד** — שמונה, כולן מסבירות את עצמן, אין `QUERY_ALL_PACKAGES`.
- **אין TODO/FIXME ואין `print` שנשכח** בכל `lib`.

---

---

# ח. סבב שני — שלושת המסכים הגדולים וחלון ווינדוס

## ✅ ח1 · מחיר עם פסיק נשמר כאפס — **בוצע**

**ביסוס: הורץ** · `lib/projects_screen.dart:847` מול `:914`

**הבדיקה והשמירה מנתחות את אותו טקסט בשתי דרכים שונות.**

```dart
// _validateMoney — הבדיקה
double.tryParse(text.replaceAll(',', '.'))

// _submit — השמירה
price: double.tryParse(_priceCtrl.text) ?? 0,
```

הרצתי את שתיהן על אותם קלטים:

| מה שהוקלד | הבדיקה אומרת | מה שנשמר |
|---|---|---|
| `40,000` | 40.0 ✔ עובר | **0** |
| `1,500` | 1.5 ✔ עובר | **0** |
| `40,5` | 40.5 ✔ עובר | **0** |
| `40000` | 40000 ✔ | 40000 ✔ |

**סופר שמקליד מחיר ספר תורה כ-`40,000` — הדרך הטבעית ביותר לכתוב את המספר הזה — מקבל פרויקט שמחירו אפס.** הטופס אמר לו שהכול תקין. מכאן והלאה כל רווח, כל שכר לשעה וכל הצעת מחיר לפרויקט הזה הם אפס.

**וזה בדיוק הבאג שההערה מעל `_validateMoney` מכריזה שתוקן:** *"הטופס נהג ליפול ל-`tryParse(...) ?? 0`, כך שטעות במחיר יצרה פרויקט שמדווח אפס רווחים לנצח, בלי ששום דבר על המסך מסביר למה."* הבדיקה נוספה — **ו-`_submit` נשאר בדיוק כפי שהיה.**

שני באגים למעשה: ‏(א) הבדיקה מפרשת פסיק אלפים כנקודה עשרונית, ולכן `40,000` הוא 40 בעיניה; ‏(ב) השמירה לא מפרשת אותו כלל ונופלת ל-0. **גם אילו הסכימו — שתיהן שגויות.**

**המלצה:** פונקציית ניתוח אחת, משותפת לבדיקה ולשמירה, שמסירה מפרידי אלפים ומקבלת נקודה בלבד כעשרוני. `expenses` באותה שורה. **מאמץ: שעה, כולל בדיקה.**

## ✅ ח2 · חלון ווינדוס — **בוצע** (מינימום, זכירת גודל, חזרה מחלון צף)

**ביסוס: נקרא** · `windows/runner/main.cpp:29`, `win32_window.cpp:138`, `lib/main.dart:46`

**התשובה הקצרה: כן, אפשר להקטין ולהגדיל בחופשיות — ובזה בדיוק הבעיה.**

החלון נוצר עם `WS_OVERLAPPEDWINDOW`, הסגנון הסטנדרטי שכולל ידיות שינוי גודל וכפתור הגדלה. **אין `minimumSize` ואין `maximumSize` בשום מקום:**

```dart
WindowOptions(size: Size(1280, 720), center: true, ...)
```

| | מצב |
|---|---|
| הקטנה | ✅ ללא גבול — **וזו תקלה** |
| הגדלה / מסך מלא | ✅ |
| גדול מהמסך (ריבוי מסכים) | ✅ |
| מסך מלא אמיתי (F11, ללא מסגרת) | ❌ לא קיים |
| זכירת הגודל בין הפעלות | ❌ לא קיים |

**שלוש המלצות:**

**ח2א · גודל מינימלי.** בלעדיו אפשר לגרור את החלון לרוחב של כמה עשרות פיקסלים והממשק נשבר. הפריסה המשורטטת עוברת לעמודה אחת מתחת ל-620px ומתחת לזה נדחסת. **שורה אחת:** `minimumSize: Size(420, 640)`. **מאמץ: דקות.**

**ח2ב · לזכור את הגודל והמיקום.** כל הפעלה פותחת 1280×720 במרכז. באפליקציית דסקטופ שעובדים בה שעות זו הטרדה יומית. **מאמץ: שעה.**

**ח2ג · החזרה מהחלון הצף דורסת את הגודל שבחרת.** `home_screen.dart:958` קובע `setSize(1280, 720)` קשיח — מי שסידר לעצמו חלון בגודל מסוים, נכנס לחלון צף וחזר, מאבד אותו. **מאמץ: חצי שעה** (לשמור לפני המעבר, לשחזר בחזרה).

## ✅ ח3 · "המשך" אחרי הפסקה אינו ממשיך — **בוצע**

**ביסוס: הורץ** · `lib/home/ruled_home_body.dart:473`

`HomeActions` מחזיק שדה `onResume`, ותיעודו אומר במפורש למה הוא קיים: *"עצירה וחידוש הם אותו כפתור בפריסת הכרטיסים, ולכן היא צריכה חידוש כפעולה בפני עצמה."*

**הוא בשימוש במקום אחד בלבד בכל האפליקציה** — `cards_smart_body.dart:196`. הפריסה המשורטטת מחווטת את הכפתור שכתוב עליו "המשך" אל **`onBreak`**:

```dart
SoferSecondaryButton(
  s.isPaused ? "המשך" : "הפסקה",
  icon: s.isPaused ? Icons.play_arrow : Icons.coffee,
  onPressed: actions.onBreak,   // ← גם כשכתוב "המשך"
),
```

ו-`onBreak` הוא `_onBreakTap`, שעושה:

| מצב | מה קורה בלחיצה על "המשך" |
|---|---|
| **רגיל** | `_pauseTimer()` שוב → נשאר מושהה, **ומאפס את תחילת ההפסקה**. אין יציאה |
| **חכם** | דיאלוג "הפסקת קפה" נפתח מחדש |

**במצב רגיל בקלף/לילה אין שום דרך לחזור לכתוב.** היציאה היחידה היא "סיים" — כלומר לסגור את הישיבה.

זה **קיים מלפני** השינויים שעשינו במסך הראשי; העברתי את הכפתורים אבל החיווט השגוי עבר איתם.

**התיקון: מילה אחת** — `onPressed: s.isPaused ? actions.onResume : actions.onBreak`, בדיוק כפי שפריסת הכרטיסים כבר עושה. **מאמץ: דקה, ועוד עשר לבדיקה שנועלת את זה.**

## ח4 · מה נמצא תקין בשלושת המסכים

- **אפס `!` (null assertion)** בשלושתם. היגיינה טובה.
- **אפס `int.parse` / `double.parse`** חשופים — הכול `tryParse`.
- **מחיקת פרויקט** (`projects_screen.dart:104`) — `copyWith(isDeleted: true)` אכן קובע `deletedAt: DateTime.now()`, כלומר המצבה נכתבת כראוי והמחיקה תשרוד מיזוג.
- **שינוי גאומטריית עמוד** מבקש אישור מפורש כשיש עבודה רשומה, ומסביר מה ישתנה.
- **`_validatePositiveInt`** דוחה אפס ושלילי — הלקח מ"ספירה מתחילה מאחת" מיושם.
- **מטבע של פרויקט קיים אינו משתנה** כשמשנים את ההגדרה (`:918`). בדיוק הכלל הנכון.

---

---

# ט. סבב שלישי — המסכים שנותרו

`summary_screen` · `project_summary_screen` · `expenses_screen` · `work_calendar_settings_screen` · `lib/project/*`

## ✅ ט1 · סיכום פרויקט מדווח שאתה כותב מהר משאתה כותב — **בוצע**

**ביסוס: נקרא** · `lib/project_summary_screen.dart:114`

```dart
Duration totalTime = Duration.zero;
for (var s in sessionsForStats) {
  totalTime += s.duration;
}
```

**המילה `timeRecorded` אינה מופיעה בקובץ הזה אף פעם אחת.**

רשומה שהוזנה בלי שעות נשמרת עם `startTime == endTime`, כלומר `duration` שלה הוא אפס — **אבל השורות שלה נספרות במלואן**. התוצאה:

| מה שמוצג | הכיוון |
|---|---|
| "דקות לשורה" | **נמוך מדי** — נראה מהיר יותר |
| ‏₪ לשעה (`profitPerHour`) | **גבוה מדי** — אותו `totalTime` במכנה |

ככל שסופר מזין יותר רשומות בלי שעות, כך שני המספרים סוטים יותר — **והוא לא יכול לדעת זאת מהמסך.**

זה בדיוק מה שהשדה `timeRecorded` נוצר כדי למנוע, והדוח החודשי אכן מכבד אותו:

```dart
// monthly_report.dart:108
if (s.timeRecorded) worked += s.duration;
```

**המסך הזה קדם לשדה ולא עודכן.** התיקון הוא תנאי אחד — אבל צריך גם להחליט מה קורה לשורות: או לספור רק שורות מרשומות שנמדד בהן זמן (עקבי, ומה שהדוח החודשי עושה), או לומר על המסך שחלק מהנתונים חלקיים. **זו החלטת מוצר ולכן לא נגעתי.** ‏**מאמץ: חצי שעה** אחרי שתחליט.

## ✅ ט2 · אותו מספר, שתי דיוקים, באותו מסך — **בוצע**

**ביסוס: נקרא** · `project_summary_screen.dart:625` מול `:693`

```dart
"${estimate.workDaysLeft.toStringAsFixed(0)} ימי עבודה"   // שורה 625
"${estimate.workDaysLeft.toStringAsFixed(1)} ימי עבודה"   // שורה 693
```

אותו נתון בדיוק מוצג במקום אחד כ-"12 ימי עבודה" ובאחר כ-"11.5 ימי עבודה". **מאמץ: דקות.**

## ✅ ט3 · תצוגת ימי ההפסקה הקרובים — **בוצע**

**ביסוס: נקרא** · `lib/work_calendar_settings_screen.dart:320`

```dart
today.add(const Duration(days: 30))
```

האתר האחרון שנותר עם החישוב הישן. ההשפעה קטנה: רק מי שפותח את המסך בסמוך לחצות, פעמיים בשנה, יראה תצוגה מקדימה של 29 ימים במקום 30. **עקביות יותר מאשר באג** — `CalendarDays.addDays` כבר קיים. **מאמץ: דקה.**

## ✅ ט4 · הגנה חסרה על רשומות מחוקות — **בוצע**

**ביסוס: נקרא** · `project_summary_screen.dart:105`

```dart
widget.history.where((s) => s.projectId == project.id)
```

בלי `!s.isDeleted`. **אין לזה השפעה היום** — הקורא (`home_screen`) מסנן מחוקים בטעינה. אבל כל שאר מסכי החישוב מסננים בעצמם, וזה היחיד שסומך על מי שקרא לו. **מאמץ: דקה.**

## ט5 · ומה שנמצא תקין

ארבעת המסכים האלה **נקיים במידה יוצאת דופן**, ובניגוד למסכים הגדולים לא נמצא בהם דבר חמור:

- **אפס `int.parse`/`double.parse`** חשופים, **אפס** `replaceAll(',', '.')` — באג המחיר לא הגיע לכאן.
- **אפס צבעים קשיחים** (`Colors.*`) בכל הארבעה. הם קוראים את הצבעים מהערכה כפי שנדרש.
- **כל החלוקות מוגנות** — `linesForStats > 0 && totalTime.inSeconds > 0` לפני כל ממוצע, בשלושת סוגי הפרויקט.
- **`null` assertion אחד בלבד** בכל הקבוצה, ואף אחד ב-`lib/project/` פרט לאחד ב-`progress_grids`.
- `sessionsForStats` **כן** מחריג `backlogOnly` משני צדי היחס, עם הערה שמסבירה למה.

---

## הערה על היקף

**הסקירה הושלמה.** כוסו: שכבת הלוגיקה, האחסון, המניפסטים, תצורת חלון ווינדוס, וכל המסכים — כולל `lib/project/*`.

מה שלא נבדק בהרצה על מכשיר: התנהגות בפועל במוד כהה, בגודל טקסט מוגדל, ועם קורא מסך. שלושתם דורשים בדיקה ידנית ולא קריאת קוד.
